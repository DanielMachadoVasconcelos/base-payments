{
  "annotations": {"list": []},
  "editable": true,
  "gnetId": null,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "type": "stat",
      "title": "Postgres Up",
      "datasource": "Prometheus",
      "targets": [
        { "expr": "pg_up" }
      ],
      "options": {"reduceOptions": {"values": false, "calcs": ["lastNotNull"]}},
      "gridPos": {"x": 0, "y": 0, "w": 6, "h": 4}
    },
    {
      "type": "graph",
      "title": "Transactions (commits/rollbacks)",
      "datasource": "Prometheus",
      "targets": [
        { "expr": "sum(rate(pg_stat_database_xact_commit[5m])) by (datname)", "legendFormat": "commit {{datname}}" },
        { "expr": "sum(rate(pg_stat_database_xact_rollback[5m])) by (datname)", "legendFormat": "rollback {{datname}}" }
      ],
      "gridPos": {"x": 6, "y": 0, "w": 18, "h": 8}
    },
    {
      "type": "graph",
      "title": "Tuples fetched/returned",
      "datasource": "Prometheus",
      "targets": [
        { "expr": "sum(rate(pg_stat_database_tup_fetched[5m])) by (datname)", "legendFormat": "fetched {{datname}}" },
        { "expr": "sum(rate(pg_stat_database_tup_returned[5m])) by (datname)", "legendFormat": "returned {{datname}}" }
      ],
      "gridPos": {"x": 0, "y": 8, "w": 24, "h": 8}
    }
  ],
  "schemaVersion": 16,
  "style": "dark",
  "tags": ["postgres", "exporter"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "timepicker": {},
  "timezone": "browser",
  "title": "PostgreSQL Overview",
  "version": 1
}
